{
  "name": "muzoo-search",
  "version": "0.0.1",
  "description": "A search for MuZoo",
  "main": "lib/search.js",
  "scripts": {
    "start": "tape test/*.js | tap-spec",
    "test": "lab -v -P test -L -c -t 0"
  },
  "dependencies": {
    "elasticsearch": "^11.0.1",
    "mu": "apparatus/mu"
  },
  "devDependencies": {
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
